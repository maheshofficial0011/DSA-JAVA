# Important Questions

1. Why does Java array indexing start at 0?
2. Why is the last index `length - 1`?
3. Why should max/min not start from 0?
4. Why is max/min O(n)?
5. Why does O(n/2) become O(n)?
6. Why does backward traversal not reverse an array?
7. Why is two-pointer reversal O(1) auxiliary space?
8. Why can `Integer.MIN_VALUE` be a dangerous sentinel?
9. What does `!hasSecondLargest` mean?
10. How does the second-largest one-pass invariant work?

### Self-Test

Explain each answer without looking at the notes.
