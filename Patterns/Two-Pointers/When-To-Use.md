# When to Use Two Pointers

Consider two pointers when:

- You need to process both ends of an array.
- The input is sorted and you are searching for a pair.
- A left and right boundary naturally move toward each other.
- Pointer movement can safely eliminate processed positions.

Always verify that pointer movement does not skip a required candidate.
