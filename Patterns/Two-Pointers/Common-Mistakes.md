# Two Pointers — Common Mistakes

- Forgetting the stopping condition.
- Moving the wrong pointer.
- Forgetting to modify the array when modification is required.
- Using an unnecessary extra array for an in-place problem.
- Confusing backward traversal with actually reversing the array.
